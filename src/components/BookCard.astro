---

import type { CollectionEntry } from 'astro:content';
// import  type {BookI } from '../ components/BookInterface';
import  type {BookI } from '../BookInterface'

interface Props {
  book: BookI;
}

const {book} = Astro.props as Props;
---

<div class="book-card">
  
  
</div>
<style>
  .book-card {
    box-shadow: 5px 4px 0px lightgray;
    padding: 1rem;
    background-color: #f5f5f5;
    color: red;
    font-size: 10px;
    margin-bottom: 1rem;
  }
  .tt {
    color: blue;
  }
</style>
